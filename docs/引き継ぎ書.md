# 引き継ぎ書（オーナーマイページ）

作成：2026年9月22日
★ この書類は社外に出さないでください。

---

## 0. この書類について

**2026年9月22日まで、この書類はどこにもありませんでした。**
仕組みの全体が、担当者の記憶の中にしかない状態でした。

この書類は、`index.html` `js/app.js` `staff.html` `css/style.css` を
1行ずつ読み、**コードから復元したもの**です。
Apps Script 側（`コード.gs` `ext.gs` `paper.gs` `pass.gs`）は、この入れものに
1行も入っていないため、**画面が呼んでいる窓口の名前と、送っている中身**
から分かることだけを書いています。→ `gas/README.md`

---

## 1. いまの状態（2026年9月22日）

| | |
|---|---|
| 画面 | できています。ログイン／ホーム／入居状況／送金明細／原状回復・修繕／火災保険／お問い合わせ |
| 当社用の画面 | できています（`staff.html`／合言葉で入ります） |
| Apps Script | 反映・デプロイ済み |
| **明細** | **まだ1件も届いていません。** PIVOT2 の［マイページへ送る］が通信を始められずにいました（原因は下の「落とし穴 ⑧」） |
| オーナー様への配布 | まだです。社内2〜3名で1週間お試しのあと |

### 公開先

```
オーナー様の画面   https://irelife.github.io/owner/
当社用の画面       https://irelife.github.io/owner/staff.html   ← 社外に出さない
```

### 全体の絵

```
    オーナー様のスマートフォン
            │
            ▼
    irelife/owner（GitHub Pages）……… 画面だけ。中身は1件も持ちません
            │  入館証（token）を付けて問い合わせ
            ▼
    Apps Script ………………………… ここで本人確認・中身の判断
            │
            ├─ スプレッドシート「オーナーマイページ 台帳」
            └─ Google ドライブ「オーナー明細」

    PIVOT2（毎月1回・当社が押す）
            │  ［マイページへ送る］＋ 合言葉（ADMIN_KEY）
            └─▶ Apps Script（putPdf → push）
```

**PIVOT・Firestore へは、オーナー様の画面から一切つながりません。**
向きは必ず PIVOT2 → マイページの一方通行です。

---

## 2. 入れものの中身

```
index.html        画面。6つの画面が1枚に入っています
css/style.css     見た目（ワインの意匠）
js/config.js      ★ Apps Script のURL・会社名・受け皿アドレス
js/app.js         画面の動き（1247行）
staff.html        当社用の画面（合言葉で入ります。URLを外に出さない）
tests/tins.cjs    火災保険の検査（node tests/tins.cjs）
docs/             はじめの設定・PIVOT側の追加・やること・使いかた・この書類
gas/README.md     Apps Script の4本を、ここに控える手順
```

**オーナー様の氏名・住所・金額・明細PDFは、1件もここにありません。**

### Apps Script の側（スプレッドシートに紐づくスクリプト）

| ファイル | 中身 | 関数の数 |
|---|---|---|
| `コード.gs` | 入口（`doPost`／`doGet`／`route`）・ログイン・再設定・`home`／`status`／`papers`／`pdf`・PIVOTから受ける `push`／`putPdf`・入館証・表のあつかい | 49 |
| `ext.gs` | 火災保険・修繕・やりとり・支社わけ・当社用の画面（`routeOwnerMore_`／`routeStaff_`） | 43 |
| `paper.gs` | 明細のまとめ（`ppKey_`／`ppYm_`／`ppDay_`／`ppYear_`／`ppYen_`／`ppRows_`／`ppItem_`／`ppChart_`）＋確認用3つ | 12 |
| `pass.gs` | 初回パスワードを出し直す（`初回パスワードを出す`） | 4 |

★ **名前のぶつかりは0件**であることを確かめてあります。

### 守りの要点（Apps Script の中）

- パスワードは保存していません。**`SHA-256(パスワード + 塩 + 胡椒)` の結果だけ**を持ちます。台帳が漏れても、胡椒（`PEPPER`）が無ければ戻せません
- PDFは共有リンクにしません。`apiPdf` が、**ご本人の明細に載っているIDのときだけ**中身を渡します
- ログインに5回失敗すると**30分止まります**（`locked_`）
- 「このメールは登録されていません」とは答えません。登録の有無を外から調べられないためです
- 明細は**3年分でおしまい**（`KEEP_YEARS = 3`、`sweepOld_` が落とします）

---

## 3. 窓口の一覧

### ① オーナー様の画面 → Apps Script（18個）

`js/app.js` が呼んでいるものすべてです。**`login` `forgot` 以外は、
すべて入館証（`token`）を付けて送ります。**

| 窓口 | 送るもの | 返るもの／すること | app.js |
|---|---|---|---|
| `login` | `email` `pass` | 入館証（32文字・14日） | 149行 |
| `logout` | `token` | 入館証を無効にする | 1217行 |
| `me` | — | `name` `atena`（入館証が生きているかの確認も兼ねます） | 1231行 |
| `forgot` | `email` | 再設定のご案内メール（★有る／無いを答え分けません） | 179行 |
| `changePass` | `cur` `next` | パスワード変更（初回・変更の両方） | 217行 |
| `home` | — | `month` `total` `sokinDate` `rows[]` `props[]` `pdfId` | 231行 |
| `status` | — | `newc[]`（新規契約）`yotei[]`（解約予定） | 269行 |
| `papers` | — | `years[]` `items[]`（過去の送金明細） | 349行 |
| `pdf` | `id` | 明細PDFの中身（base64）★共有リンクにしません | 538行 |
| `works` | — | 原状回復・修繕の一覧 | 665行 |
| `workMsg` | `id` `body` | その工事について連絡する | 734行 |
| `talks` | — | やりとり（吹き出し） | 562行 |
| `talkMsg` | `id` `body` | やりとりに送る | 616行 |
| `ask` | `kind` `body` | お問い合わせ（支社の担当へ） | 635行 |
| `insList` | — | 火災保険の証券の一覧 | 891行 |
| `insPut` | `b64` `name` `mime` `prop` `maker` `tel` `mail` `no` `until` | 証券を預かる | 1144行 |
| `insFile` | `id` | 証券の写しの中身（base64） | 1088行 |
| `insDrop` | `id` | 証券を消す | 1106行 |

`home` が返す `props[]` は `{ name, note }` の形です。
**火災保険の物件の候補は、ここから作っています**（`insCands`）。

### ② 当社用の画面 → Apps Script（4個）

`staff.html` が呼びます。**入館証ではなく合言葉（`ADMIN_KEY`）**を付けます。

| 窓口 | 送るもの | すること | staff.html |
|---|---|---|---|
| `stPing` | `key` | 合言葉が合っているかを確かめる | 197行 |
| `stList` | `area` `open` | 問い合わせの一覧（支社で絞る／未返信だけ） | 223行 |
| `stArea` | `mail` `area` | その方の支社を決める | 312行 |
| `stReply` | `id` `body` `mail` | 返信する（マイページに吹き出しで出ます） | 327行 |

### ③ PIVOT2 → Apps Script（2個）

`pivot2/js/tomypage.js` が呼びます。**合言葉（`ADMIN_KEY`）**を付けます。

| 窓口 | 送るもの | すること |
|---|---|---|
| `putPdf` | `key` `name` `b64` | 明細PDFをドライブに入れ、ファイルIDを返す |
| `push` | `key` `owners[]` | オーナー・明細・入居状況を書き込む（同じ月は置き換え） |

`owners[]` の1件の形（`tomypage.js` の `shape()`）：

```
{ email, name, atena, ym, total, sokinDate,
  rows:[{label, amount}],
  status:[{kind, prop, room, date, note}],
  fileId }
```

★ `コード.gs` の **`contact` と `threads` は使われていません。**
　 `ext.gs` の `ask`／`talks` に置き換わっています。消してよいものです。

**合計 24個。** 通信はすべて `POST` ／ `Content-Type: text/plain`。
`application/json` にすると、ブラウザが先に許可を聞く通信を投げ、
Apps Script が答えられずに失敗します（落とし穴 ①）。

---

## 4. 表（シート）の一覧

スプレッドシート「**オーナーマイページ 台帳**」の中。

### `はじめの用意` で作られるもの（6つ）

★ **2026/9/22 に6つめが分かりました。`入館証` です**（それまで未確認でした）。

| 表 | 列 |
|---|---|
| `オーナー` | メール／氏名／宛名／塩／合言葉／初回変更／作成日時／**支社** |
| `明細` | メール／対象月／ファイルID／表示名／**送金額**／**送金日**／**内訳**／登録日時 |
| `入居状況` | メール／対象月／種類／物件／部屋／日付／補足 |
| `問い合わせ` | 日時／メール／用件／内容／状況／**番号** |
| `入館証` | 合言葉／メール／期限 |
| `ログイン試行` | 日時／メール／結果（同じIDで5回まちがえると30分待ち） |

### はじめて使われたときに自動でできるもの（3つ）

| 表 | 列 |
|---|---|
| `保険` | メール／物件／保険会社／証券番号／満期日／ファイルID／表示名／登録日時／**連絡先**／**保険会社メール** |
| `工事` | 番号／メール／物件／**部屋**／内容／着手日／完了日／金額／相殺予定／状況／備考／登録日時 |
| `やりとり` | 番号／日時／差出／本文（**工事とお問い合わせで共通**） |

★ `保険` の「連絡先」「保険会社メール」は**表のいちばん右**です（あとから足したためです）。

★ **`入居状況` には `物件` と `部屋` が別の列で入っています。**
　 つまり `status` 窓口が `prop`／`room` を返せるようにするのは、
　 列を足す話ではなく、返す形に足すだけです（→ `docs/TODO.md` の4番）。

先に作っておきたいときは、Apps Script で **`追加の表を用意する`** を実行します。

**列の並びは見た目だけの話です。中身は列の名前で読んでいます。**

### ドライブ

```
オーナー明細（フォルダ）… 明細PDF ／ 火災保険の証券の写し
```

★ **共有リンクにしません。** Apps Script が読んで、ご本人にだけ渡します。

---

## 5. スクリプト プロパティ

Apps Script の ⚙️ プロジェクトの設定 → スクリプト プロパティ。

| 名前 | 中身 | 状態 |
|---|---|---|
| `PEPPER` | パスワードの胡椒 | ★自動で作られます。**絶対に外に出さない** |
| `ADMIN_KEY` | 当社用の画面・PIVOT2 の合言葉 | ★自動で作られます。**絶対に外に出さない** |
| `SUPPORT` | 受け皿のアドレス | `info@ire-life.com` |
| `SITE_URL` | `https://irelife.github.io/owner/` | 入っています |
| `DRIVE_ID` | 「オーナー明細」フォルダのID | 入っています |
| `MAIL_FUKUYAMA` | 福山の問い合わせ先 | **空** |
| `MAIL_KURASHIKI` | 倉敷・総社の問い合わせ先 | **空** |
| `MAIL_OKAYAMA` | 岡山の問い合わせ先 | **空** |

**プロパティを足すだけで効きます。貼り直しもデプロイのやり直しも要りません。**

---

## 6. 踏んだ落とし穴

| | 何が起きたか | なぜか／どう直したか |
|---|---|---|
| ① | Apps Script への通信が、すべて失敗した | `Content-Type: application/json` にすると、ブラウザが先に「許可を聞く通信」を投げます。Apps Script はそれに答えられません。**`text/plain` にします**（`js/app.js` 12〜16行目の但し書き） |
| ② | 直したのに、1台も新しくならない | `index.html` の `?v=` を上げていませんでした。**`js/` `css/` を直したら必ず1つ上げます** |
| ③ | 公開リポジトリに、オーナー様の実名とメールが入っていた | 2026/9/17 に取り除きました（オーナー37件・メール12件・お名前11名）。`pivot2/js/ownermail.js` 7〜14行目に経緯が残っています。**`pivot2` `pivot3` は公開です** |
| ④ | ボタンが2回目から「送信中…」のままになった | `busy()` が2回目の呼び出しで「送信中…」を元の字として覚えていました。`dataset.busy` で二重呼びを防いでいます（`js/app.js` 46〜62行目） |
| ⑤ | パスワードを見せたまま、次の画面へ行けた | 画面を移るときに必ず隠しに戻します（`eyeReset()`。`show()` から呼びます） |
| ⑥ | 入居状況の札に、日付が出ていた | サーバーが返す `tag` は解約予定のとき日付です。札は「解約予定」と書き、日付は下の行に回しました（`mvRows()`） |
| ⑦ | マージ済みの PR に push しても、入らなかった | GitHub の PR 画面は、閉じたあとも開いていたときとほぼ同じ見た目で残ります。**新しい枝と新しい PR を作ります** |
| ⑧ | **PIVOT2 の［マイページへ送る］が、通信を1回も始めなかった** | `pivot2/js/ownermail.js` は全体が囲い（IIFE）の中にあり、`detail`（仕分け結果）と `makeOwnerPdfBase64` は**外から名前で呼べません**。`window.RENT` の一覧にも入っていませんでした。`tomypage.js` は外から `detail` を読もうとして毎回 `null` になり、「送るものがありません」で止まっていました。→ `window.RENT` に読み取り口を2つ足し、`tomypage.js` は読めなかったときに `localStorage` から読むようにしました |
| ⑨ | **ホームのカードが、飾りのない箱になっていた** | PR #4 の1つ目のコミットで `css/style.css` を範囲ごと置き換えたとき、あいだにあった `.tiles` `.tile` `.tile:hover` `.tile-k` `.tile-t` `.tile-a` `.tile-d` の**7つをまるごと消していました**。`js/app.js` でも古い `paintChart` が後ろに残り、新しいほうを打ち消していました。**関数宣言は後ろ勝ちで、構文エラーも出ません。**→ **ブロックごと置き換えたら、消えたセレクタと、関数の重複を必ず数えてください** |
| ⑩ | 貼り替えたら構文エラーになった | `コード.gs` の353行目を、**行のとちゅうから**選んで貼り替えたため、次の行とくっつきました。**必ず行まるごと**選んでください |
| ⑪ | 「2026年9月」が日付になっていた | `appendRow` で入れると、日本語ロケールでは**日付として保存**されます。読む側で両方うけとるようにしてあります（`ppKey_`／`ppYm_`／`ppDay_`）。**PIVOT2 から送るときも同じことが起きます** |
| ⑫ | 入居状況を押すとログイン画面に戻された | `routeStaff_` が `action.indexOf('st') !== 0` で判定していたため、**`status` を当社用とまちがえて**いました。名前を並べて確かめる形に直しました（`stPing`／`stList`／`stReply`／`stArea`） |
| ⑬ | ホームに先月ぶんが出たまま | 月の並べ替えが `localeCompare` の**文字くらべ**だったため、「2026年9月」が「2026年10月」よりあとに来ていました。`ppKey_` の数くらべに直しました（3か所） |
| ⑭ | 空欄の送金額が「0円」と出た | `Number('')` が **0** になるためです。`ppYen_` で `null` にしました |
| ⑮ | **ログインしても、すぐログイン画面に戻った** | `show('home')` は `home`・`status`・`papers` の**3つを同時に叩きます**。`auth()` は、どれか1つでも「入館証が違う」を返すと `logout(true)` を呼びます。**`quiet=true` なので何も出ないまま戻ります。**しかも `status` と `papers` の失敗は呼び出し側が握りつぶすので、**メッセージすら残りませんでした。**→ ホームの「ついで」（`status`／`papers`）は失敗しても戻さない／戻すときは理由を必ず出す／どの窓口で起きたかを `console.warn` に残す。検査は `tests/tlogin.cjs`（12件） |
| ⑯ | **`?v=` を上げたのに、直しが届かなかった** | `?v=` は `css` と `js` には効きますが、**`index.html` 自身には付けられません。**GitHub Pages は `index.html` を10分ほどブラウザに覚えさせます（`Cache-Control: max-age=600`）。古い `index.html` が使われると、その中の古い `?v=` を読むので、**新しい `app.js` がいつまでも読まれません。**→ 直した直後に確かめるときは、**シークレットウィンドウ**（`Ctrl+Shift+N`）で開くのが確実です。`Ctrl+Shift+R` でも消えないことがあります |
| ⑰ | **`status` を押すとログイン画面に戻った**（2026/9/22。落とし穴⑫は**直っていませんでした**） | 原因は **`ext.gs` の 68行目**です（`コード.gs` ではありません。`コード.gs` にあるのは呼び出しだけです）。`if(String(action \|\| '').indexOf('st') !== 0) return null;` のため、`status` が当社用と判定され、合言葉の検査に進んで**「合言葉が違います（`error:'auth'`）」**が返っていました。→ **2026/9/22 に直しました。**`var STAFF = ['stPing','stList','stReply','stArea']; if(STAFF.indexOf(String(action \|\| '')) < 0) return null;` ★名前で確かめるので、将来 `st` で始まる窓口を足しても起きません。**直したら「デプロイを管理 → 鉛筆 → 新バージョン」** |
| ⑱ | 入力欄を押さないと出ないため、画面が空っぽに見えた | 火災保険とお問い合わせは、**入力欄を最初から開いた形**にしました（`<details open>`／`#in-form-host` を隠さない）。1件も預けていない方には、何もない画面に見えていました |
| ⑲ | 火災保険の一覧に、CSS が1行も当たっていなかった | `.item` `.row-btn` `.x` `.t` `.s` が `css/style.css` に無く、素の文字が並んでいました。2026/9/22 の作り替えで直しました（ホームの物件一覧も同じ理由で直しました） |

---

⑳ **実物を見ずに、直しかたを書いてはいけません。**
　 私は `ext.gs` を読まずに「番号を言い当てた方が他のオーナー様のやりとりに書き込める」「番号を作っているのは3か所」と書きました。**どちらも誤りでした。**
　 実物には、どの窓口にもご本人確認が入っており、ぶつかるのは `apiAsk` の1か所だけでした。
　 そのかわり、私が気づいていなかった**当社用の入口の足止めなし**が見つかりました。
　 ★**Apps Script の4本は、直す前に必ずチャットに貼っていただく。**
　 公開リポジトリには置けないので、控えは共有ドライブか非公開リポジトリに。

## 7. 残っている仕事

| | 何を | 決めていただくこと |
|---|---|---|
| **0** | **★当社用の入口（`routeStaff_`）に足止めがない（未修正・いちばん急ぎ）** — `ADMIN_KEY` は**何回でも試せます**。オーナー様のログインは5回で30分止まるのに、こちらは止まりません。`stList` は**全オーナー様のお名前・メール・お問い合わせ本文（最大200件）**を返し、Apps Script の住所は `js/config.js`（**公開リポジトリ**）にあります | **まず `ADMIN_KEY` を40文字の意味のない文字列に作り直す**（これで穴はふさがります）。足止めは保険。→ `docs/番号のぶつかりの直しかた.md` 1章 |
| **0-2** | **★`apiAsk` の番号のぶつかり（未修正）** — `'C' + 日付 + '-' + (最終行+1)` のため、**ふたりがほぼ同時に送ると同じ番号**になります。`やりとり` にメールの列がなく `msgsOf_` は番号だけで引くので、**別のオーナー様のやりとりが混ざって見えます**（読むときだけ。書き込みは守られています） | `newNo_` を足して3か所を差し替え。→ 同じ手順書2章 |
| **0-3** | **★`stReply` が、お知らせ先を画面から受け取っている（未修正）** — 担当者が一覧で選び間違えると、**別のオーナー様に「お返事を入れました」**が届きます | 番号から引き直す形に差し替え。→ 同じ手順書3章 |
| 1 | **試し送り** — `pivot2` PR #21 をマージ → 社内アドレス1件で［マイページへ送る］を押す | Apps Script 側（`push` `putPdf`）が受けられるかは、押すまで分かりません |
| 2 | **支社ごとの問い合わせ先** — `MAIL_FUKUYAMA` `MAIL_KURASHIKI` `MAIL_OKAYAMA` | それぞれのアドレス。振り分けの規則は `docs/TODO.md` に確定済み |
| 3 | **会社のアドレスを1つに揃える** — 3つ混在 | どれを生かすか。→ `docs/メールアドレス.md` |
| 4 | **台帳の6つめのシート名を確かめる**（上の 4. 参照） | — |
| 5 | **★取り下げました。Apps Script を `gas/` に控えるのは、できません** — `irelife/owner` は**公開リポジトリ**です（2026/9/22 確認）。`ext.gs` の先頭に「社外に出さないでください」とあります。私が公開・非公開を確かめずに書いた宿題でした | 控えるなら ①owner を非公開に（Pages は Pro 以上が必要）②`gas` 用の非公開リポジトリを作る ③共有ドライブに置く。→ 手順書7章 |
| 6 | **満期日を、前に文字で入れられたぶんの手直し** | 新しい画面は `2028-03-31` の形で送ります。前の `2028年3月31日` もそのまま読めるので、急ぎではありません |
| 7 | **`me` 窓口に `email` を足す** — いまマイアカウントのメールアドレスは、ログイン時に控えた `localStorage` の値です。窓口が返せば、そちらが本筋になります（16章） | `ext.gs`／`コード.gs` のどちらに `me` があるか確かめてから。1行です |
| 8 | **ご登録内容の変更を、当社側で受けやすくする** — いまはお問い合わせ（`kind='登録内容の変更'`）に文章で届きます。件数が増えるなら専用の表が要ります | まず数か月、お問い合わせのままで様子を見るのがおすすめです |
| 9 | **第2段**（30％保証の状況・空室状況）・**第3段**（契約の進み具合・清掃報告・お知らせ） | いつ着手するか |

---

## 8. ほかのアプリの宿題（2026年9月22日 現在）

| アプリ | 宿題 | 状態 |
|---|---|---|
| **pivot2** | ［マイページへ送る］が通信を始めない | **PR #21 で直しました。マージ待ち** |
| **pivot3** | 30％保証の日割りを PIVOT2 とそろえる | **保留**（2026/9/22 ご指示）。PR #15 は**下書き**のまま置いてあります |
| **checkin** | PR #9 | **マージ済み**（9/22 03:18）。宿題は終わっています |
| **全部** | 会社のアドレスが3つ混在 | → `docs/メールアドレス.md`。**お決めいただく段** |
| **kintai** | — | 今回は触っていません |
| **crm** | いま止めています | アドレスが10か所。揃えるときに直します |

### pivot3 の直しについて（★いまは保留です。記録として残します）

**2026/9/22 のご指示で、いったん見送りになりました。**
PR #15 は**下書き**のまま置いてあります。着手のご指示があれば、
下書きを外すだけで進められます（検査も CI も緑の状態で止めてあります）。

以下は、見送った中身の記録です。

`planOf()` が「月をそのまま切った額」を LINE に渡していたため、

```
例）9月15日から保証・募集賃料65,000円
    LINE が言う額 … 19,500円
    実際の送金　　 … 29,900円（9月の端数 10,400円を10月にまとめるため）
    9月の10,400円 … どの月のお知らせにも出ていなかった
```

**日割りの規則そのものは変えていません。**
`splitRange` `monthPay` `firstBill` は、もともと PIVOT2 と1文字も同じでした。
変えたのは「LINE に何を渡すか」だけです。

★ **`gPlan` は物件を保存したときに作られます。**
　 すでに保存されている物件は、開いて保存し直すまで古い額を持ったままです。

★ **PIVOT2 は、この直しがすでに入っています（9/21・PR #19）。**
　 ですので pivot3 を保留にしても、**PIVOT2 のほうは、
　 9/21 以降に保存し直していない物件があると古い額のままです。**
　 pivot3 の保留とは別の話として、一度ご確認ください。

---

## 9. Base44（React）版から抜き取った仕様（2026年9月22日）

### いまの立ち位置（お決めいただいたこと）

**オーナーマイページは、いま2つあります。**

| | 中身 | 立ち位置 |
|---|---|---|
| **`irelife/owner`** | 素のJS＋Apps Script。GitHub Pages で公開中 | ★**本線。**これをオーナー様にお配りします |
| **Base44 の React 版** | `src/pages/*.jsx`・`base44.entities.*`・shadcn/ui・jsPDF | **見た目と仕様の見本**としてのみ使います |

★ **Base44 版は GitHub のどのリポジトリにも入っていません。**
　 `.jsx` は5リポジトリに1つもなく、`package.json` も検査用の2つだけです。
　 `css/style.css` の4行目に「いただいた React（base44）の見た目を、そのまま
　 写しています」と書いてあるのが、唯一の手がかりです。

★ **PIVOT2 の［マイページへ送る］は、本線（Apps Script）にしか送りません。**
　 Base44 の `Statement` には1件も入りません。

### 入居状況（`Status.jsx`）から抜き取ったもの

| 抜き取ったもの | 改良前（本線） | 改良後 |
|---|---|---|
| **物件ごとにまとめる** | 状態ごと（新規契約／解約予定／募集中）に縦に並び、**物件を横断して混ざっていた** | **1物件＝1つのまとまり。**物件名の見出しの下に、その物件の部屋が並びます |
| **部屋の並び** | サーバーが返した順 | **募集中 → 解約予定 → 新規契約**、同じ状態なら号室順（`10号室` が `201号室` より前） |
| **物件の並び** | なし | 名前順。物件名の無いものはいちばん下 |
| **まとめの一行** | なし | **「募集中 4室 ／ 解約予定 2室 ／ 新規契約 1室 ございます。」** |
| **募集中の札** | 新規契約と同じ見た目で見分けられなかった | 金色で目に留まるように |

### ★ 解約予定日が過ぎた部屋（2026年9月22日 お決めいただいたこと）

**解約予定日が過ぎた部屋は「募集中」として扱います。**

```
改良前： 2026年3月に解約予定だった部屋が、9月になっても
         「解約予定」のまま出続けていました。
         時間が経つほど、画面が信用できなくなります。

改良後： 解約予定日が過ぎていれば、募集中として出します。
         そのとき、理由もいっしょに出します。
           「解約予定日（2026年8月31日）を過ぎたため、
             募集中としております。」
```

★ **日付が読めないときは、動かしません。**「解約予定」のまま残します。
　 推測でお金や状態を動かさない、というきまりに合わせています。

★ この判定は画面側で行っています。**`解約予定` シートの中身は変えていません。**
　 ですので、あとで「やっぱり残したい」となっても、画面を戻すだけです。

### 物件と部屋の分けかた（いまは画面側でやっています）

本線の `status` 窓口は `place` を1本の文字列で返します（「カルムコート東棟 201号室」）。
そのため、うしろが `…号室` の形のときだけ切り分けています。

```
カルムコート東棟 201号室  → 物件「カルムコート東棟」／部屋「201号室」
カルムコート東棟201号室    → 同じ（空白が無くても切れます）
マーベラス B101号室        → 物件「マーベラス」／部屋「B101号室」
グロリオサ 東棟            → 物件「グロリオサ 東棟」／部屋なし
                             ★空白だけで切ると「東棟」が部屋になるため、
                               「号室」の形が無いときは切りません
```

★ **`status` 窓口が `prop` と `room` を別々に返すようにすれば、
　 切り分けは不要になります。**（PIVOT2 はすでにその形で送っています。
　 GAS が `place` に1本化しているだけです）
　 その場合も画面側の直しは要りません。`prop` があればそちらを使います。

### 送金明細（`Papers.jsx`）から抜き取ったもの

| 抜き取ったもの | 改良前（本線） | 改良後 |
|---|---|---|
| **月で並べ替える** | サーバーが返した順をそのまま使っていた。順が崩れると、いちばん大きなカードに違う月が出る | `ppSort()` で新しい月から順に。**1つでも読めない月があれば並べ替えません**（勝手に動かさないため） |
| **CSV の列** | 「項目,金額」の2列。そのあとに「送金日」の行が続き、表の形が崩れていた | **「対象月,送金日,項目,金額」の4列。**何か月ぶんを1つの表に貼っても、どの月の行か分かります |
| **金額の下ぞろえ** | 月の字が2行になったカードだけ、金額の高さがずれた | 3枚ならびは金額を下ぞろえに（React の `h-44` ＋ `mt-auto` と同じ考え） |
| **月を折り返さない** | 「2026年10月」が途中で折れる余地があった | `word-break:keep-all`（React の `break-keep`） |
| **ファイル名の言葉** | `…ぶん 収支明細.csv`（画面は「送金明細」なのに食い違い） | `…ぶん 送金明細.csv` |

### 抜き取らなかったもの（理由つき）

| 抜き取らなかったもの | 理由 |
|---|---|
| **`pdf_url` で原本PDFを直接ダウンロード** | **共有リンクになります。**ご指示の「明細PDFは共有リンクにしません。Apps Script が読んで、ご本人にだけ渡します」に反します。本線は `pdf` 窓口が base64 を返し、画面が Blob にする形のままにしました |
| **jsPDF で画面側からPDFを作る** | **日本語が1文字も出ません。**jsPDF の既定フォント（helvetica）は CJK の字を持っていません。出すには NotoSansJP の埋め込み（字を絞っても数百KB）が必要です。本線のPDFは Apps Script が作るので、この問題がありません |
| **金額を `\` で書く書きかた** | React 版の `` `\\${…}` `` はテンプレート文字列でバックスラッシュ1つになり、**`¥1,284,000` ではなく `\1,284,000` と出ます** |
| **CSV で金額まで引用符で囲む** | Excel が文字として読むことがあります。本線は**文字は囲み、数は囲みません** |
| **`.catch` を書かない読み込み** | React 版は通信が落ちても「データがありません」と出ます。**オーナー様には「まだ無い」と読めますが、実際は繋がっていません。**本線は失敗した理由を画面に出します |
| **読めない月を `0` として並べる** | `0` が2つ以上あると並びが不定になり、**黙って違う月が先頭に来ます。**本線は `null` を返し、読めない月があれば並べ替えません |
| **選んだ月のカードが閉じたまま** | React 版は `expanded` に最新の `id` しか入れないため、月を選ぶと金額だけ出て、もう一度押さないと内わけが見えません。本線は選んだら開いて出ます |
| **改ページの無いPDF** | React 版は `y` を足し続けるだけで `addPage()` がなく、A4 の下端を越えたぶんが静かに消えます |

### まだ抜き取っていない仕様（Apps Script 側の足しが必要です）

**`Statement.message`（その月の、当社からのひとこと）。**
Base44 版は明細1件ごとに `message` を持ち、PDFの末尾に載せています。

本線で同じことをするには、`明細` シートに `備考` の列を足し、
`papers` が `message` を返すようにする必要があります。
**画面側だけ先に作っても、何も出ません。**ですので、いまは入れていません。

★ 入れると良い場面：「10月は原状回復の精算があるため、いつもより少なくなります」
　 といったご説明を、明細のところに残せます。いまは「お問い合わせ」で
　 別に書くしかありません。

---

## 10. 火災保険の新しい作り（2026年9月22日）

| | 改良前 | 改良後 |
|---|---|---|
| 並び | 1証券＝1行のべた並び。同じ物件の証券が離れて並ぶ | **1物件＝1つの箱。**箱の中に、その物件の証券をまとめる |
| 足しかた | いつも同じ入力欄が1組、画面の上にある | **［＋ 別の物件を追加する］で箱ごと／［＋ この物件に証券を追加する］で箱の中に** |
| 物件 | **任意**。空のまま預けられ、まとまらなかった | **必須。**明細から分かる物件名から選ぶ（無ければ手入力） |
| 満期 | 文字。並べ替えも、お知らせもできない | **日付。**満期の近い箱が上に来て、過ぎている／90日以内で札が変わる |
| 連絡先 | 文字だけ | **［電話をかける］**が出る（番号のところだけが押せます） |
| 見た目 | CSS が1行も当たっていなかった | 箱・札・行に CSS を当てた |
| 検査 | なし | `tests/tins.cjs`（69件） |

**サーバーの窓口は1つも増やしていません。** `insList` が返す `prop`（物件名）で
まとめているだけです。Apps Script の貼り直し・デプロイのやり直しは要りません。

### 物件名のまとめかた

`insNorm()` で、こうそろえてから見比べます。

```
カルムコート 東棟  ┐
カルムコート　東棟  ├→ すべて同じ物件として、1つの箱に入ります
ｶﾙﾑｺｰﾄ東棟        ┘
カルムコート西棟   → 別の物件（東と西は分けます）
```

### 満期日

```
読めるもの： 2028-03-31 ／ 2028/3/31 ／ 2028年3月31日 ／ 20280331 ／ 2028年3月
読まないもの： 令和10年3月31日 ／ 来年の3月 ／ 満期は代理店に確認中
```

**読めないものは、推測しません。**「満期日が未記入」の札を出し、
書かれた文字はそのまま画面に出します（消しません）。

### まだできないこと

- **預けたものの「直し」**（満期日だけ訂正・証券の差し替え）。
  いまは消して入れ直していただく形です。Apps Script に `insEdit` を足せば作れます
- **満期のお知らせメール**。画面で見えるだけです

### 直すときに気をつけること

**入力欄（`#in-form-host`）を `#in-boxes` の中に入れないでください。**
一覧を描き直すたびに `innerHTML` で作り直すため、中に入れると
入力欄ごと消え、`#f-ins` `#in-go` も一緒に無くなって二度と送れなくなります。
`index.html` の但し書きにも書いてあります。

---

## 11. 確認用に、手で実行できるもの

Apps Script の画面で、実行するものを選んで［実行］。結果は「実行ログ」に出ます。

| 名前 | ファイル | 何をするか |
|---|---|---|
| `はじめの用意` | コード.gs | 表を作り、`PEPPER`・`ADMIN_KEY` を用意 |
| `動作テスト` | コード.gs | 自分あてにテストのアカウント（★`fileId` が空なので**明細は入りません**） |
| `追加の表を用意する` | ext.gs | 保険・工事・やりとりの表、支社・番号の列 |
| `明細を確かめる` | paper.gs | 画面へ渡る中身を、そのまま出す |
| `明細シートを数える` | paper.gs | 明細が何件入っているか |
| `明細のお試しを入れる` | paper.gs | 自分あてに12か月ぶん（★本物には入りません） |
| `明細のお試しを消す` | paper.gs | 「お試し」と付いたものだけ消す |
| `初回パスワードを出す` | pass.gs | 初回パスワードを出し直す |

★ **本番のデータを触るときは、必ず先に `明細を確かめる` で見てから。**

★ `pass.gs` の `PASS_SEND = true` で**もとからいる**オーナー様に送ると、
　 「このたびご用意いたしました」という**開設のご案内**が届きます。
　 再発行の文面と分ける必要があります（`made` で分岐）。

---

## 12. 決め待ち（ご判断をいただきたいこと）

| | 内容 | いまの状態 |
|---|---|---|
| 1 | ロゴの字。`IREライフ` か `Owner's Salon` か | `IREライフ` |
| 2 | 福山・倉敷・岡山の問い合わせ先アドレス | 未設定（全部 `SUPPORT` へ） |
| 3 | 会社のアドレスを、どれに揃えるか | 3つ混在。→ `docs/メールアドレス.md` |
| 4 | 「税理士へ送信」を、**自動で添付して送る**形にするか | **2026/9/22、GAS を触らずに作れるところまで作りました**（下の14章）。自動添付と「12か月を1つのPDF」には、GAS の窓口が要ります |
| 5 | 入居者のお名前を出すか | いまの `status` は返しません。出すなら PIVOT2 から送る必要があります |
| 6 | 配りはじめる時期 | 社内2〜3名で1週間 → 全員、と決定済み |

---

## 13. 図の色（2026年9月22日）

年間の収支グラフの色は、**目で選ばずに確かめた値**です。

| | 見本の値 | 何が問題だったか | 直した値 |
|---|---|---|---|
| 収入 | `#6f2c3a` | カードの地との明暗差 **1.3:1**（読める線は3:1）。棒が地に沈む | 金 `#E2B357`（**6.7:1**） |
| 支出 | `#b8b8b8` | 累積 `#c59868` との隔たり **ΔE 11.0**（下限は15） | 青 `#5C9FB5`（**ΔE 22.4**） |

`css/style.css` の `:root` に `--inc` `--out` として置いてあります。
配色（15章）を足したので、**配色ごとに置き直して**あります。

| 配色 | 収入 | 支出 | 収入と支出の隔たり ΔE |
|---|---|---|---|
| ワイン | `#E2B357` | `#5C9FB5` | 75.1 |
| ミッドナイト | `#E2B357` | `#4E93AD` | 77.9 |
| チャコール | `#E0B96F` | `#55A0B8` | 67.4 |


★ **色だけに頼らないよう**、グラフの下に「数値で表示」の表を付けてあります。
★ 図を2つに分けたのは、収入（約53万円）と累積収支（12か月で約580万円）が
　 **11倍ちがう**ためです。1つの図に左右2本のものさしを置くと
　 「今月の収入と、これまでの累計が同じくらい」と読めてしまいます。

---

## 14. 税理士へ送信（2026年9月22日）

ホームの6枚目のカードから入ります。**入口だけだったところに、中身を作りました。**

### できること（当社側の用意は要りません）

| | |
|---|---|
| 税理士先生のご連絡先 | 事務所名・お名前・メールを、**この端末の中だけ**に覚えます（`localStorage` の `ire_owner_tax`）。**当社へは送りません** |
| 1か月ぶん／1年ぶん | 切り替えられます。年は明細にある年から選べます |
| **表（CSV）** | 1か月ぶんでも1年ぶんでも保存できます |
| **明細書（PDF）** | **1か月ぶんだけ。**Apps Script が作った**原本**をそのまま保存します |
| メール | 件名と本文の入った状態で、お使いのメールソフトが開きます |

### できないこと（Apps Script に窓口が1つ必要です）

| | なぜ |
|---|---|
| **PDFを自動で添付して送る** | ブラウザからは添付できません。メールソフトの決まりです。いまは「保存 → 手で添付」の2手間になります |
| **12か月を1つのPDFにまとめる** | 原本PDFを結合する必要があります。★**画面側で1から作ってはいけません。**日本語の字を持っていないため、月名も項目名も出ません |

★ ですので**1年ぶんのときは、PDFのボタンを出しません。**本文も「表（CSV）を添付しております」に変えます（付けられないものを本文で約束しないためです）。

### 表（CSV）の形が、送金明細の画面とは違います

| | 送金明細の画面 | 税理士へ送信 |
|---|---|---|
| 列 | 対象月,送金日,項目,金額（**4列**） | 対象月,送金日,**区分**,項目,金額（**5列**） |

**「区分」を足した理由。** 何か月ぶんも1つの表にすると、`内わけ` の行と `ご送金額` の行が混ざります。区分が無いと、**金額の列をそのまま合計したときに二重に足します。**
税理士先生にお渡しする表なので、そこは曖昧にしません。**`区分＝内わけ` だけで絞れば、正しく合計できます。**

2か月ぶん以上のときだけ、いちばん下に `合計` の行を足します（対象月は空にします）。

### 検査

`tests/tacc.cjs`（**43件**）。年の取り出しと並び・CSVの形・件名と本文・メールソフトを開く文字。

★ `acMailto` は、改行を **`%0D%0A`** にしています。`%0A` だけでは改行されないメールソフトがあるためです。
★ メールを開くのは `location.href` ではなく `<a>` を押す形です。スクリプトから `location` を書き替えるのを止めるブラウザがあるためです。

### 自動で送る形にするなら

Apps Script に窓口を1つ足します。

```
taxSend（入館証が要る）
  受けとる … mode（'month' か 'year'）／ym か year／to／subject／body
  すること … その方の明細PDFを（年なら12か月ぶんを結合して）取り出し、
             CSV を作り、MailApp で添付して送る
  返す　　 … { ok:true, sent:1 }
```

★ **PDFの結合も Apps Script 側で行ってください。**画面側（jsPDF）で作ると、日本語が1文字も出ません。
★ 税理士先生の宛先は、いまこの端末にしか覚えていません。自動送信にするなら `オーナー` シートに列を足して覚えるのが筋です。

---

## 15. 文面の言い直し（2026年9月22日）

オーナー様は50代以上の方が中心です。**言い換えて易しくしたつもりの言葉が、
かえって幼く見えていました。** 画面に出る文面を、ひととおり業務の言葉に直しました。

| 直す前 | 直した後 | なぜ |
|---|---|---|
| この内容で預ける／お預かりしているもの／預かっています… | この内容で登録する／ご登録済みの火災保険／登録しています… | 「預ける」は比喩。登録という事実の言葉にしました |
| この端末に覚える／覚えました | この端末に保存する／保存しました | 同上 |
| 消します／消しました | 削除します／削除しました | 同上 |
| 〜を入れてください／お入れください | 〜をご入力ください | 命令形の言い換えを標準の敬語へ |
| パスワードを決めてください | パスワードをご設定ください | 同上 |
| **入館証**を受け取れませんでした | **認証情報**を取得できませんでした | 社内の呼び名が画面に漏れていました |
| うまくいきませんでした | 処理できませんでした | |
| 電波の良いところで、もう一度 | 通信環境をご確認のうえ、もう一度 | |
| 2つの欄が違います | 新しいパスワードが一致しません | どの欄か分かるように |
| いまのパスワード／もう一度 | 現在のパスワード／新しいパスワード（確認） | |
| 1か月ぶん／1年ぶん | 月別／年別 | |
| 中身の確認／お送りする／メールを作る | 送付内容の確認／送信／メールを作成する | |
| 内わけ | 内訳 | CSV の「区分」の値も直しました |
| 表（CSV） | 明細データ（CSV） | 税理士事務所へのメール本文も同じに |
| 税理士先生のご連絡先／先生のお名前 | 税理士事務所のご連絡先／ご担当者名 | |
| 新しくご相談する／これまでのご相談 | 新規のお問い合わせ／お問い合わせ履歴 | |
| ご自由にお書きください | ご質問・ご要望をご記入ください | |
| お返事をお待ちください | 担当者より回答いたします | 当社の責任の所在をはっきり |
| 長すぎます。2000文字までに | 文字数が上限を超えています。2,000文字以内で | |
| 数字で見る | 数値で表示 | |
| いつ／補足／相殺の予定 | 期間／備考／相殺予定 | 工事の見出し |
| 満期日が未記入／満期日をいただいていない | 満期日 未登録／満期日が未登録の物件 | |
| 「募集中 1室 **ございます**。」 | 「募集中 1室」 | 日本語として通っていませんでした |
| いつでもどうぞ。どんなことでも。 | ご質問・ご要望をお送りください | |
| やめる | キャンセル | |

★ **コードの中のコメント**（`js/app.js` の「窓口」「入館証」「検査できる道具」など）は
　 直していません。社内の言葉として引き継ぎ書と対になっているためで、
　 オーナー様には表示されません。

★ CSV の値（`内訳`）を直したので、`tests/tacc.cjs` の期待値も直しました。

---

## 16. マイアカウント（2026年9月22日）

右上の **⋮ →［マイアカウント］** から入ります。
以前は⋮に［パスワードを変える］だけが並んでいました。

★ **Apps Script は1行も足していません。** いまある窓口だけで作っています。

### 4つのカード

| | 中身 | 使っている窓口 |
|---|---|---|
| ① ご登録情報 | お名前・宛名・メールアドレス（**表示だけ**） | `me`（お名前・宛名）＋ `localStorage` の `ire_owner_mail` |
| ② パスワードの変更 | 現在／新しい／確認 | `changePass`（もとからあるもの） |
| ③ 配色 | ワイン／ミッドナイト／チャコール | **窓口なし。** この端末の中だけ（`ire_owner_theme`） |
| ④ ご登録内容の変更 | メール・電話・郵便番号・住所・備考 | `ask`（お問い合わせ）へ `kind='登録内容の変更'` で送る |

### メールアドレスがなぜ `localStorage` なのか

`me` 窓口は **お名前と宛名しか返しません**。メールアドレスは返りません。
そこで**ログインに成功した時点のアドレスを控えて**、この画面で出しています。
ログアウトで消します（`ire_owner_token` と同時）。

★ 直すなら、`me` に `email` を1つ足すのが本筋です（7章の宿題に入れました）。

### ④が「申請」で、直接書き換えでない理由

1. **オーナー表にお電話番号・ご住所の列がありません。** 列を足して書き込む
　 窓口を作るには Apps Script の変更が要ります。
2. **ご登録のアドレスは、ログインの鍵そのものです。** 画面から書き換えられると、
　 打ち間違いでご本人が入れなくなります。**当社が確認してから**書き換えます。

本文は `myBody` が組みます。メールアドレスは必ず **「前 → 後」**の形で書きます
（当社側でどちらが新しいのか迷わないようにするためです）。

### プロフィール写真を作らなかった理由

見本（Base44）は `UploadPublicFile` を使っていました。これは
**URL を知っていれば誰でも開ける場所**へ上げる作りです。オーナー様のお顔の写真を
その形で扱うわけにはいきません。**当社の窓口を通して非公開で預かる形**にしない限り、
作りません。

### 配色

`css/style.css` の `html[data-theme="…"]` で、地・カード・文字・差し色・図の色を
まとめて差し替えます。`:root` は既定のワインのままにしてあります
（`data-theme` が付く前の一瞬でも、色が抜けないようにするためです）。

色は目で選んでいません。次を計算して確かめてあります。

| 見るところ | 必要 | ワイン | ミッドナイト | チャコール |
|---|---|---|---|---|
| 本文の文字／地 | 4.5:1 | 12.3 | 14.4 | 13.8 |
| 薄い文字／カード | 4.5:1 | 6.1 | 7.2 | 6.9 |
| 差し色／カード | 3.0:1 | 5.2 | 7.1 | 5.4 |
| ボタンの文字／差し色 | 4.5:1 | 5.9 | 8.2 | 6.4 |
| 図 収入・支出／カード | 3.0:1 | 6.7／4.4 | 7.6／4.3 | 6.8／4.6 |

★ 隣り合って並ぶ図の2色だけは、**明暗差ではなく色の隔たり（ΔE）**で見ます。
　 13章の表のとおり、3つとも下限15を大きく超えています。

★ `index.html` の `<head>` に**先あての `<script>`**を1つ置いています。
　 `app.js` を待つと、ワイン以外をお選びの方に一瞬ワインが見えるためです。
　 **ここの色の値は `js/app.js` の `THEMES` と対です。片方を直したら、もう片方も直してください。**

### 検査

`tests/tmy.cjs`（**69件**）。配色の選び直し・全角の直し・お電話番号・郵便番号・
メールアドレス・入力の確かめ・お問い合わせ本文を見ています。

★ お電話番号と郵便番号は、**桁が合わないときに直したり足したりしません**。
　 推測でお直しすると、つながらない番号を当社が正しいものとして持ってしまうためです。
